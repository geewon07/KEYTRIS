--- # local 설정
spring:
  config:
    activate:
      on-profile: "db-local"
  data:
    redis:
      host: localhost
      port: 6379
      password:

--- # dev 설정
spring:
  config:
    activate:
      on-profile: "db-dev"
  data:
    redis:
      host: keytris-redis
      port: 8380
      username: develop
      password: "ssafy@a401"

--- # prod 설정
spring:
  config:
    activate:
      on-profile: "db-prod"
  data:
    redis:
      host: keytris-redis
      port: 8379
      username: develop
      password: "ssafy@a401"